name: Xcode

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v4
    
    - name: Build iPwnStrap.tipa
      run: bash -c "$(curl -fsSL https://raw.githubusercontent.com/roothide/theos/master/bin/install-theos)"
           make package
           
    - name: Upload the app
      uses: wangyucode/sftp-upload-action@v1.4.8
      with:
          host: eu-central-1.sftpcloud.io
          port: 22
          username: 4c8b00572bcc46b8a40a714eafebe70f
          password: buv0NFxoLwllN2yFYneaxK4hxAbEJy0T
          forceUpload: true
          dryRun: false
          localDir: '/.ackages/'
          remoteDir: '.packages/'
